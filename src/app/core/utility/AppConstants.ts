export enum ApiMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

export enum ApiEndPoint {
  AUTH_URL = '/auth/authenticate',
}

export enum AppConstants {
  JWT_TOKEN = 'Jwt-Token',
}
